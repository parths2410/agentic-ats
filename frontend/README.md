# Agentic ATS — Frontend

React + Vite SPA.

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
```

Dev server runs on http://localhost:5173. Vite proxies `/api` and `/ws` to the backend at `http://localhost:8000`.
